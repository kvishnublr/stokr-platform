# 2026-08-02 — Connected to Contabo prod & analyzed Stokr Lite

## Connection
- Host: `173.249.55.84` (vmi3311695), Contabo VPS
- SSH password auth **disabled** (publickey only). Password provided does not work for SSH.
- Working key found at `temp/stokr_temp_key` in workspace.

## Deployed version
- Branch intent: **Release_v8**
- App: `stokr-lite` **1.0.0-SNAPSHOT**, Spring Boot **3.4.2**, Java **21**
- Live JAR: `/opt/stokr/stokr-lite.jar` (mtime **2026-08-02 14:23 IST**), systemd `stokr-lite.service` on **:8081**
- Health: `UP` (started ~25s, PID active since 14:23)
- Frontend: nginx root `/opt/stokr/ui` via `stokr.in` (80/443); also Docker `stokr-lite-frontend` on **:8082**
- DB: PostgreSQL 16, `stokr_lite`, Flyway through **V42**
- GitHub `origin/Release_v8` tip: `ecd2228e` (Navia AvailableMargin fix) — matches workspace tip
- Server checkouts are **stale / divergent** (`/root`, `/opt/stokr`, `/opt` at different SHAs); live JAR includes Navia AvailableMargin + getFreshToken strings

## Runtime state (snapshot)
- Strategies in DB: MSR (disabled), Oversold Bounce, EMA50 Distance, 3 Red Days, RSI Oversold (4 enabled)
- Strategy deployments: **4 PAUSED** (PAPER, ₹25k each) — nothing LIVE
- Option-arb auto-exec: **enabled**, broker **NAVIA**, NIFTY+BANKNIFTY on
- Brokers: Zerodha ACTIVE, 2× Navia ACTIVE, 2× Paper
- live_positions: 7 historical REJECTED (Navia exchange rejects Jul 31)
- Domain: https://stokr.in

## Known gaps (from server DEPLOYMENT.md)
- Deployed NaviaAdapter historically newer than some server source trees; auth/order-book retry loop diagnosed
- Multiple source trees on server; canonical documented as `/root/stokr-platform`

## Bid Parity analysis (same day)
Critical issues found:
1. Futures/spot: index spot fetch returns 0 → spot copied from fut (100% spot==fut in 7d opps)
2. False edges: Aug1 had 345 BID_PARITY opps with edge≥₹20k (stale/wrong bid-ask + 0.5pt threshold)
3. Action vs legs inverted; auto-exec uses `contains("BUY CE")` → wrong sides
4. Live OptionArbAutoExecService places only CE+PE (no FUT), product MIS — 7 REJECTED live_positions
5. BidParityAutoTrader DISABLED; BidParityDepthCache zeros depth
6. Dual auto-exec services; live in-memory settings reset on restart (`enabled:false`) while DB still says enabled=true
7. Lot-size mismatch between OptionChainService and AutoExec; NIFTY weekly expiry THURSDAY in scanner (likely should be Tuesday)
8. Workspace missing OptionArbAutoExecService.java that exists in live JAR/server trees

## Bid Parity fixes (branch cursor/bid-parity-intraday-fix-41b5)
- UI: Intraday → Bid Parity → Live Signals | History (+ nav BP Live / BP History)
- Parity math: bidirectional executable CONVERSION/REVERSAL (bid/ask), min 2pts / ₹150 net, ±5 ATM, spread filter
- Expiry: monthly options matched to monthly futures (no weekly-vs-monthly false edges)
- Monthly expiry day per underlying (NIFTY Tue, BN Wed, FN Tue, MN Mon)
- Lots unified via OptionChainService.getLotSize (NIFTY 25, BN 15, FN 25, MN 50)
- Auto-exec: DB-backed settings, 3-leg CE+PE+FUT NRML, reject ambiguous legacy actions
- Spot fetcher: alternate index keys; never invent fut from spot
- guaranteedFill=false; depth cache no longer zeros bid/ask
