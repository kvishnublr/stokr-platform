stokr.in, www.stokr.in {
    encode gzip zstd

    # Route everything to stokr-lite nginx (host network via docker bridge gateway)
    reverse_proxy 172.18.0.1:8082
}

new.stokr.in {
    encode gzip zstd
    reverse_proxy stokr-ui:80
}
