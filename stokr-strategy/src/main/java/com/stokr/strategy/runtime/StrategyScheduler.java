package com.stokr.strategy.runtime;

import org.springframework.stereotype.Component;

@Component
public class StrategyScheduler {
    // Phase 1: in-memory / manual triggers; extend with @Scheduled when needed
}
