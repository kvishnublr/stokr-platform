package com.stokr.strategy.dto;

import java.math.BigDecimal;
import java.util.UUID;

/**
 * Trader-facing view of execution config.
 * Admin-controlled fields (executionMode, liveEnabled, paperEnabled) are included
 * for display only ??? they are not writable via the trader endpoint.
 */
public record TraderExecutionConfigDto(
        UUID id,
        String strategyKey,
        /** true = no personal override exists yet; values shown are admin global defaults */
        boolean isGlobalFallback,

        /** Catalog definition id when strategy exists in strategy_definitions. */
        UUID definitionId,
        String displayName,
        String riskLevel,

        // ?????? Admin-set (read-only for trader) ????????????????????????????????????????????????????????????????????????????????????????????????
        String executionMode,
        boolean liveEnabled,
        boolean paperEnabled,

        // ?????? Trader-editable ???????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
        boolean enabled,
        boolean telegramEnabled,
        boolean forceFixedQty,
        BigDecimal fixedQty,
        int maxPositions,
        BigDecimal dailyLossLimit,
        int cooldownMinutes,
        boolean allowPyramiding,
        boolean emergencyStopEnabled
) {}
